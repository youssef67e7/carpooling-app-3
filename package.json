{
  "name": "weret",
  "private": true,
  "description": "WERET monorepo: npm start = Expo from /mobile. npm run backend for API.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix mobile",
    "mobile": "node scripts/start-mobile.cjs",
    "backend": "npm run dev --prefix backend",
    "start": "node scripts/start-mobile.cjs",
    "expo": "node scripts/start-mobile.cjs",
    "expo:start": "node scripts/start-mobile.cjs",
    "api-proxy": "npm run api-proxy --prefix mobile",
    "dev": "concurrently -n backend,mobile -c blue,green \"npm run backend\" \"npm run mobile\"",
    "dev:lan": "concurrently -n backend,proxy,mobile -c blue,green,magenta \"npm run backend\" \"npm run api-proxy\" \"npm run mobile\"",
    "test": "npm test --prefix backend && npm run test:smoke --prefix backend",
    "test:unit": "npm test --prefix backend",
    "test:smoke": "npm run test:smoke --prefix backend",
    "setup:firebase": "node scripts/setup-firebase.cjs",
    "apk": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-apk.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "expo-auth-session": "~55.0.16",
    "expo-crypto": "~55.0.15",
    "expo-web-browser": "~55.0.16"
  }
}
