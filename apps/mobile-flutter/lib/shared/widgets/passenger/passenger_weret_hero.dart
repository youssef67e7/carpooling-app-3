import 'package:flutter/material.dart';

/// Migrated from React Native: components/passenger/PassengerWeretHero.js
class PassengerWeretHero extends StatelessWidget {
  const PassengerWeretHero({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from PassengerWeretHero
  }
}
