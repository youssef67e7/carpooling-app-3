import 'package:flutter/material.dart';

/// Migrated from React Native: components/passenger/PassengerMapPickerModal.js
class PassengerMapPickerModal extends StatelessWidget {
  const PassengerMapPickerModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from PassengerMapPickerModal
  }
}
