import 'package:flutter/material.dart';

/// Migrated from React Native: components/ui/weret/WeretScreenHeader.js
class WeretScreenHeader extends StatelessWidget {
  const WeretScreenHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from WeretScreenHeader
  }
}
