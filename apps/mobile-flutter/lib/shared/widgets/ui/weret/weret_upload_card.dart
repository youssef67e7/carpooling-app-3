import 'package:flutter/material.dart';

/// Migrated from React Native: components/ui/weret/WeretUploadCard.js
class WeretUploadCard extends StatelessWidget {
  const WeretUploadCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from WeretUploadCard
  }
}
