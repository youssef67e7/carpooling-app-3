import 'package:flutter/material.dart';

/// Migrated from React Native: components/ui/StaggerEntrance.js
class StaggerEntrance extends StatelessWidget {
  const StaggerEntrance({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from StaggerEntrance
  }
}
