import 'package:flutter/material.dart';

/// Migrated from React Native: components/ui/FormErrorCallout.js
class FormErrorCallout extends StatelessWidget {
  const FormErrorCallout({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from FormErrorCallout
  }
}
