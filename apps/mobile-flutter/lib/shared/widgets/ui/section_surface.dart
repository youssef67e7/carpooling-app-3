import 'package:flutter/material.dart';

/// Migrated from React Native: components/ui/SectionSurface.js
class SectionSurface extends StatelessWidget {
  const SectionSurface({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from SectionSurface
  }
}
