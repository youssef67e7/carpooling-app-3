import 'package:flutter/material.dart';

/// Migrated from React Native: components/auth/WeretOutlineWordmark.js
class WeretOutlineWordmark extends StatelessWidget {
  const WeretOutlineWordmark({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: port UI from WeretOutlineWordmark
  }
}
