import 'package:flutter/material.dart';
import '../../../shared/widgets/weret_screen_scaffold.dart';

/// Migrated from React Native: screens/AdminDashboardScreen.js
/// Status: generated stub — wire business logic from mobile-legacy reference.
class AdminDashboardScreen extends StatelessWidget {
  const AdminDashboardScreen({super.key});

  static const routeName = 'AdminDashboardScreen';

  @override
  Widget build(BuildContext context) {
    return const WeretScreenScaffold(
      title: 'AdminDashboard',
      rnSource: 'screens/AdminDashboardScreen.js',
      child: Center(
        child: Text('AdminDashboardScreen — migrated route preserved'),
      ),
    );
  }
}
