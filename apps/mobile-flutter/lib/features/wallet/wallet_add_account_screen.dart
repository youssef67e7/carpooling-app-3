import 'package:flutter/material.dart';
import '../../../shared/widgets/weret_screen_scaffold.dart';

/// Migrated from React Native: screens/wallet/WalletAddAccountScreen.js
/// Status: generated stub — wire business logic from mobile-legacy reference.
class WalletAddAccountScreen extends StatelessWidget {
  const WalletAddAccountScreen({super.key});

  static const routeName = 'WalletAddAccountScreen';

  @override
  Widget build(BuildContext context) {
    return const WeretScreenScaffold(
      title: 'WalletAddAccount',
      rnSource: 'screens/wallet/WalletAddAccountScreen.js',
      child: Center(
        child: Text('WalletAddAccountScreen — migrated route preserved'),
      ),
    );
  }
}
