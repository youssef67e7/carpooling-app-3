import 'package:flutter/material.dart';
import '../../../shared/widgets/weret_screen_scaffold.dart';

/// Migrated from React Native: screens/more/SafetyTipsScreen.js
/// Status: generated stub — wire business logic from mobile-legacy reference.
class SafetyTipsScreen extends StatelessWidget {
  const SafetyTipsScreen({super.key});

  static const routeName = 'SafetyTipsScreen';

  @override
  Widget build(BuildContext context) {
    return const WeretScreenScaffold(
      title: 'SafetyTips',
      rnSource: 'screens/more/SafetyTipsScreen.js',
      child: Center(
        child: Text('SafetyTipsScreen — migrated route preserved'),
      ),
    );
  }
}
