import 'package:flutter/material.dart';
import '../../../shared/widgets/weret_screen_scaffold.dart';

/// Migrated from React Native: screens/more/AboutWeretScreen.js
/// Status: generated stub — wire business logic from mobile-legacy reference.
class AboutWeretScreen extends StatelessWidget {
  const AboutWeretScreen({super.key});

  static const routeName = 'AboutWeretScreen';

  @override
  Widget build(BuildContext context) {
    return const WeretScreenScaffold(
      title: 'AboutWeret',
      rnSource: 'screens/more/AboutWeretScreen.js',
      child: Center(
        child: Text('AboutWeretScreen — migrated route preserved'),
      ),
    );
  }
}
