import 'package:flutter/material.dart';
import '../../../shared/widgets/weret_screen_scaffold.dart';

/// Migrated from React Native: screens/more/HelpCenterScreen.js
/// Status: generated stub — wire business logic from mobile-legacy reference.
class HelpCenterScreen extends StatelessWidget {
  const HelpCenterScreen({super.key});

  static const routeName = 'HelpCenterScreen';

  @override
  Widget build(BuildContext context) {
    return const WeretScreenScaffold(
      title: 'HelpCenter',
      rnSource: 'screens/more/HelpCenterScreen.js',
      child: Center(
        child: Text('HelpCenterScreen — migrated route preserved'),
      ),
    );
  }
}
